BUILD_3.1.15.5_HOTFIX_README.txt
=================================
PreferredHome API — Full Rebuild Build 3.1.15.5
Generated: March 2026
Repo: PreferredHome-api (MAIN branch)

CHANGED FILES (in folder order)
---------------------------------
preferredhome_api/core/config_constants.py
preferredhome_api/core/models.py
preferredhome_api/utils/helpers.py
preferredhome_api/storage/sheets_storage.py
main.py

UNCHANGED FILES (correct as-is — copied verbatim)
---------------------------------------------------
preferredhome_api/core/settings.py
preferredhome_api/core/__init__.py
preferredhome_api/storage/__init__.py
preferredhome_api/utils/__init__.py
preferredhome_api/__init__.py
render.yaml
requirements.txt
.gitignore
.gitattributes

ALL BUGS FIXED IN THIS BUILD
------------------------------

BUG 1 — NaN save error (BLOCKING — was reported by user)
  File: main.py
  Root cause: _NUMERIC_FIELDS in 3.1.15.4 used old plain English names
  ("Monthly Rent", "Security Deposit") but sheet columns are camelCase
  ("baseRent", "securityDeposit"). Sanitize function never matched anything.
  Fix: _sanitize_payload() now uses cfg.NUMERIC_FIELDS directly (camelCase).
  Applied twice — before and after key mapping — to catch all edge cases.

BUG 2 — STATUS_OPTIONS outdated (config_constants.py)
  Old Streamlit statuses still present: Available, Inquired, Visited,
  No Longer Available, Not Interested.
  Fix: Replaced with correct 10-status pipeline matching mobile exactly:
  New, Contacted, Scheduled, Viewed, Shortlisted, Applied,
  Approved, Signed, Rejected, Archived.

BUG 3 — STATUS_COLORS outdated (config_constants.py)
  Colors mapped to old statuses. New statuses had no colors.
  Fix: New color map matching mobile colors.ts exactly:
  New/Contacted/Scheduled/Viewed/Applied/Shortlisted = blue/amber
  Approved = green, Signed = teal, Rejected = red, Archived = grey.

BUG 4 — APP_TITLE wrong (config_constants.py)
  Was "Apartment Tracker". Fixed to "PreferredHome".

BUG 5 — helpers.py used old plain English field names
  calculate_total_monthly() referenced "Monthly Rent", "Parking Fee" etc.
  get_comparison_color() referenced "Monthly Rent", "Square Footage" etc.
  These functions would return 0 / wrong colors for every listing.
  Fix: All field references updated to camelCase matching LISTINGS_COLUMNS:
  baseRent, parkingFee, squareFootage, commuteTime, walkScore, etc.

BUG 6 — models.py Listing alias wrong
  Field alias was "ID" (uppercase). Sheet column is "id" (lowercase).
  Fix: alias changed to "id".

DEPLOY STEPS
-------------
THIS IS A FULL FOLDER REPLACEMENT — not just one file.

1. Copy ALL files from this ZIP into your PreferredHome-api repo,
   preserving the folder structure (overwrite everything)
2. Do NOT delete the .git folder
3. Commit via GitHub Desktop:
   Build 3.1.15.5 — Full API rebuild: fix NaN save, statuses, colors, field names, app title
4. Push to GitHub
5. Render dashboard → Manual Deploy → Deploy latest commit
6. Wait ~60 seconds for green Live status
7. Verify: open https://preferredhome-api.onrender.com/health
   Expected response: {"ok":"PreferredHome API 3.1.15.5"}
8. Test: add listing with blank numeric fields — must save without error

COMMIT MESSAGE
--------------
Build 3.1.15.5 — Full API rebuild: fix NaN save, statuses, colors, field names, app title
